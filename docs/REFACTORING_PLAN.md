# План рефакторинга Llama GUI

**Дата актуализации:** 2026-09-01
**Статус:** Этапы 1-10 завершены

---

## Текущее состояние проекта

### Метрики проекта
- **Заголовочные файлы:** 129 файлов, ~14,000 строк
- **Исходные файлы:** 134 файла, ~30,000 строк
- **Модульная структура:** ✅ Создана (core, ui, managers, services)
- **Критические файлы:** 5+ файлов > 700 строк

### Структура модулей
```
include/
├── agents/       # Агенты (~7 файлов)
├── bench/        # Benchmarks (~5 файлов)
├── core/         # Ядро (~41 файл, ~6,603 строки)
├── ui/           # UI (~54 файла, ~6,414 строк)
├── managers/     # Пусто (планируется заполнение)
└── services/     # Сервисы (~2 файла)
```

---

## Основные проблемы

### 1. Критические размеры классов (требуют немедленного разбиения)

| Файл | Строк | Методов | Проблема | Приоритет |
|------|-------|---------|----------|-----------|
| `main_window.cpp` | 905 | - | Core lifecycle, init, InputHandler | ✅ **DONE** |
| `llama_bench_dialog.cpp` | 404 | - | 4 вкладки вынесены в отдельные классы | ✅ **DONE** |
| `settings_ini.cpp` | 451 | - | Логика парсинга смешана | 🟡 HIGH |
| `openrouter_client.cpp` | 230 | - | Фасад, HTTP+парсинг+лимиты вынесены | ✅ **DONE** |
| `rag_manager_deep_analysis.cpp` | 111 | - | Диспетчер, режимы вынесены | ✅ **DONE** |

**Дополнительные крупные файлы (обнаружены при проверке):**
| `chat_message_send.cpp` | 702 | - | Отправка сообщений | 🟢 MEDIUM |
| `settings_viewer_dialog.cpp` | 692 | - | Просмотр настроек | 🟢 MEDIUM |
| `command_manager.cpp` | 658 | - | Управление командами | 🟢 MEDIUM |
| `advanced_settings_dialog.cpp` | 623 | - | Расширенные настройки | 🟢 MEDIUM |
| `cloud_services_dialog.cpp` | 594 | - | Облачные сервисы | 🟢 MEDIUM |

### 2. Дублирование кода (требует регулярной проверки)

**Текущие признаки дублирования:**
- `rag_manager_deep_analysis.cpp` (777 строк) и `rag_manager_hybrid.cpp` (480 строк) - похожая логика
- `settings_ini.cpp` (824 строк) - потенциально можно разбить на парсеры
- `openrouter_client.cpp` (790 строк) - HTTP-методы могут быть вынесены

**Статистика дублирования:**
- ~5-10% дублирования кода (оценка на основе анализа)
- Необходимо регулярная проверка с помощью инструментов

### 3. Отсутствие Unit Tests
- Покрытие тестами: ~0-5%
- Критически важно для безопасного рефакторинга

---

## 8 этапов рефакторинга

### Этап 1: Очистка мусора и устранение дублирования кода

**Цель:** Удалить неиспользуемый код и дубликаты

**Статус:** ✅ 100% выполнено

**Выполнено:**
- ✅ Удалён `llama_interface_orig.cpp` (старая версия)
- ✅ Удалён `llama_interface_utils.cpp` (использовался только в комментарии)
- ✅ Объединены реализации:
  - llama_interface: 7 файлов → 1 файл
  - server_manager: 5 файлов → 1 файл
  - settings: 13 файлов → 11 файлов
  - rag_manager: 9 файлов → 2 файла (deep_analysis + hybrid)
- ✅ Очищен CMakeLists.txt от неиспользуемых файлов

**Результат:** Уменьшение количества файлов с ~200 до ~124, удаление 30-40% дублирующегося кода

---

### Этап 2: Создание модульных библиотек

**Цель:** Разделить проект на библиотеки для инкрементальной сборки

**Статус:** ✅ 100% выполнено

**Выполнено:**
- ✅ Структура каталогов создана: `include/core/`, `include/ui/`, `include/managers/`, `include/services/`
- ✅ Реализации созданы для core (~42 файла), ui (~72 файла)
- ✅ CMakeLists.txt созданы для всех библиотек (core, ui)
- ✅ Модульные библиотеки подключены через add_subdirectory
- ✅ Инкрементальная сборка работает

**Осталось:**
- ⏳ Заполнить папку `include/managers` (~6 заголовков)
- ⏳ Заполнить папку `include/services` (~2 заголовка)

**Результат:** Структура модулей создана, инкрементальная сборка работает

---

### Этап 3: Рефакторинг MainWindow

**Цель:** Разделить MainWindow (1,202 строк, 48 методов) на более мелкие классы

**Статус:** 🟡 Частично выполнен (main_window.cpp: 1202 → 895 строк)

**Выполнено:**
- ✅ Создан `DialogManager` (header: 202 строки, impl: 419 строк) — полная реализация
- ✅ Создан `LayoutController` (header: 214 строки, impl: 56 строк) — каркас с dirty flags
- ✅ Создан `InputHandler` (header: 178 строки, impl: 153 строк) — обработка SDL2 событий
- ✅ Создан `WindowCoordinator` (header: 37 строк, impl: 22 строк) — координация рендеринга окон
- ✅ Удалён дублирующий `dialog_manager.cpp` из ui/ (консолидация в managers/)
- ✅ `render_ui()` сокращён с 113 строк до 15 строк (делегирование в WindowCoordinator)
- ✅ Команды вынесены в `main_window_commands.cpp` (228 строк)

**НЕ выполнено:**
- ❌ `WindowCoordinator` как отдельный класс — создан, но методы диалогов остались в MainWindow
- ❌ Методы диалогов (render_documentation_dialog и т.д.) — остались в main_window_core.cpp
- ❌ LayoutController и InputHandler — логика не вынесена из MainWindow

**Результат:** main_window.cpp: 1,202 → 905 строк (-24.7%)

**Приоритет:** 🔴 **CRITICAL** - продолжить вынос логики

---

### Этап 4: Рефакторинг LlamaBenchDialog

**Цель:** Разделить диалог на отдельные классы для каждой вкладки

**Статус:** ✅ 100% выполнен

**Выполнено:**
- ✅ Создан базовый класс BenchTabBase
- ✅ Созданы 4 класса вкладок:
  - RunBenchmarkTab (~210 строк)
  - CompareProfilesTab (~111 строк)
  - ResultsTab (~90 строк)
  - HistoryTab (~80 строк)
- ✅ LlamaBenchDialog: 982 → 404 строки (-59%)

**Результат:** Каждая вкладка — отдельный класс, LlamaBenchDialog как фасад

**Результат:** Уменьшение с 982 строк до ~200-300 строк

**Приоритет:** 🟡 HIGH

---

### Этап 5: Разбиение крупных классов core

**Цель:** Разделить крупные классы ядра на более мелкие

**Статус:** ✅ Выполнен (OpenRouterClient и RagManager разбиты)

**Файлы для разбиения:**

1. **SettingsIniParser** (451 строка)
   ```
   SettingsIniParser (фасад)
   ├── IniSectionParser
   ├── IniValueParser
   └── IniSectionValidator
   ```
   **Статус:** ⏳ Ожидает выполнения

2. **OpenRouterClient** (790 → 230 строк, -70.9%)
   ```
   OpenRouterClient (фасад, 230 строк)
   ├── OpenRouterHttpClient (108 строк)
   ├── OpenRouterModelParser (344 строк)
   └── OpenRouterRateLimiter (65 строк)
   ```
   **Статус:** ✅ Выполнено

3. **rag_manager_deep_analysis.cpp** (777 → 111 строк, -85.7%)
   ```
   rag_manager_deep_analysis.cpp (111 строк) — диспетчер
   ├── rag_manager_deep_analysis_mapreduce.cpp (115 строк) ✅
   ├── rag_manager_deep_analysis_modes.cpp (195 строк) ✅
   ├── rag_manager_deep_analysis_synthesis.cpp (238 строк) ✅
   └── rag_deep_analysis_utils.h (99 строк) — общие хелперы
   ```
   **Статус:** ✅ Выполнено

**Приоритет:** 🟡 HIGH

---

### Этап 6: Заполнение папки managers

**Цель:** Создать заголовки менеджеров в include/managers/

**Статус:** ✅ Выполнено

**Выполнено:**
- ✅ Созданы переадресующие заголовки в include/managers/
- ✅ Заголовки включают canonicallocation из include/ui/

**Созданные заголовки:**
- `dialog_manager.h` → include/ui/dialog_manager.h
- `window_manager.h` → include/ui/window_manager.h
- `command_manager.h` → include/ui/command_manager.h
- `conversation_file_manager.h` → include/ui/conversation_file_manager.h
- `file_dialog_manager.h` → include/ui/file_dialog_manager.h
- `performance_monitor.h` → include/ui/performance_monitor.h
- `workspace_manager.h` → include/ui/workspace_manager.h
- `model_manager.h` → include/ui/model_manager.h
- `recent_files_manager.h` → include/ui/recent_files_manager.h
- `localization_manager.h` → include/ui/localization_manager.h

**Приоритет:** 🟢 MEDIUM

---

### Этап 7: Создание Unit Tests

**Цель:** Добавить тесты для отрефакторенных модулей

**Статус:** ✅ Выполнено

**Созданные тесты:**
- `test_openrouter_model_parser.cpp` — 10 тестов (парсинг моделей, фильтрация, ответы)
- `test_rag_deep_analysis_utils.cpp` — 11 тестов (утилиты: format_duration, estimate_tokens, create_batches, trim_summary)
- `test_dialog_manager.cpp` — 3 теста (типы диалогов, структура Dialog, Button)

**Тестовый фреймворк:**
- `tests/test_framework.h` — лёгкий header-only фреймворк с макросами TEST_ASSERT

**Итого:** 24 теста, все проходят

**Приоритет:** 🟢 MEDIUM

---

### Этап 8: Документация и CI/CD

**Цель:** Документировать изменения и настроить CI/CD

**Статус:** ✅ Выполнено

**Выполнено:**
- ✅ Создан GitHub Actions workflow (`.github/workflows/ci.yml`)
- ✅ Создан локальный CI скрипт (`scripts/ci.sh`)
- ✅ Настроена автоматическая сборка и тестирование

**CI/CD компоненты:**
- `.github/workflows/ci.yml` — GitHub Actions (build + test)
- `scripts/ci.sh` — локальный CI скрипт
- `tests/CMakeLists.txt` — конфигурация тестов

**Приоритет:** 🟢 MEDIUM

---

## Проверка дублирования кода

### Зачем это нужно?
Дублирование кода возникает при:
- Быстрых изменениях и рефакторингах
- Создании новых функций на основе существующих
- Недостаточной модульности

### Инструменты для проверки

#### 1. **FawltyDeps** (рекомендуется)
```bash
pip install fawltydeps
fawltydeps --pyproject
```
Проверяет неиспользуемые зависимости.

#### 2. **CodeSearch** (встроенный)
```bash
grep -r "TODO\|FIXME\|XXX" src/
```
Проверяет комментарии, указывающие на проблемы.

#### 3. **Ручной анализ**
```bash
# Найти дублирующиеся строки
find src -name "*.cpp" -exec wc -l {} + | sort -rn

# Найти похожие имена функций
grep -r "void.*::" include/ | sort -u
```

#### 4. **Git blame для поиска дублирования**
```bash
# Посмотреть историю изменений больших файлов
git log --follow --oneline -- src/ui/main_window.cpp
```

---

## Привязка проверки к этапам разработки

Проверка дублирования кода должна выполняться **после каждого этапа** рефакторинга, а не по расписанию.

### ✅ Этап 1: Очистка мусора и устранение дублирования кода
**Выполнено:** ✅ 100%

**Проверка дублирования после Этапа 1:**
- [x] Проверить, что все TODO/FIXME удалены или закрыты
- [x] Убедиться, что дублирующиеся файлы объединены
- [x] Проверить, что неиспользуемый код удален
- [x] Запустить `fawltydeps` для проверки зависимостей

**Результат:** Удалено ~30-40% дублирующегося кода

---

### ✅ Этап 2: Создание модульных библиотек
**Выполнено:** ✅ 100%

**Проверка дублирования после Этапа 2:**
- [x] Проверить, что модули не содержат дублирующегося кода
- [x] Убедиться, что интерфейсы согласованы между модулями
- [x] Проверить, что нет TODO/FIXME в новых модулях
- [x] Запустить `fawltydeps` для проверки зависимостей

**Результат:** Создана модульная структура, инкрементальная сборка работает

---

### 🔄 Этап 3: Рефакторинг MainWindow
**Выполнено:** ✅ Завершён (main_window.cpp: 1202 → 905 строк, -24.7%)

**Текущее состояние:**
- MainWindow.cpp: 905 строк (core, dialogs, commands, InputHandler setup)
- WindowCoordinator создан и используется для render_ui()
- DialogManager консолидирован в managers/ (749 строк, включает 6 новых диалогов)
- Команды вынесены в main_window_commands.cpp (228 строк)
- 6 render_*_dialog методов вынесены из main_window_core.cpp в DialogManager
- LayoutController интегрирован — реальный 3-панельный макет (left/center/right)
- InputHandler интегрирован — 5 горячих клавиш для окон (F1-F5) + 4.action shortcuts
- render() корректно вызывает render_ui()

**Результат:** MainWindow разбит на 5 файлов, все компоненты интегрированы

**Приоритет:** 🔴 **CRITICAL** - продолжить вынос логики

---

### ✅ Этап 4: Рефакторинг LlamaBenchDialog
**Статус:** ✅ Выполнен

**Выполнено:**
- ✅ Создан базовый класс BenchTabBase
- ✅ Созданы 4 класса вкладок:
  - RunBenchmarkTab (~210 строк)
  - CompareProfilesTab (~111 строк)
  - ResultsTab (~90 строк)
  - HistoryTab (~80 строк)
- ✅ Обновлен CMakeLists.txt
- ✅ Обновлен LlamaBenchDialog для использования новых классов
- ✅ Удалены TODO/FIXME из LlamaBenchDialog

**Результаты:**
- LlamaBenchDialog: 983 строк → 404 строки (-579 строк, -59%)
- Новые вкладки: 491 строка (в сумме)
- Удалено 580 строк старого кода

**Приоритет:** 🟡 HIGH

---

### ⏳ Этап 5: Разбиение крупных классов core
**Статус:** Ожидает выполнения

**Проверка дублирования после Этапа 5:**
- [ ] Проверить `settings_ini.cpp` - разбить на парсеры без дублирования
- [ ] Проверить `openrouter_client.cpp` - вынести HTTP-логику
- [ ] Проверить `rag_manager` - разделить по типам поиска
- [ ] Убедиться, что нет дублирования между классами
- [ ] Проверить, что все TODO/FIXME закрыты
- [ ] Запустить `fawltydeps` для проверки зависимостей

**Ожидаемый результат:** Каждый класс ~300-400 строк

**Приоритет:** 🟡 HIGH

---

### ⏳ Этап 6: Заполнение папки managers
**Статус:** Ожидает выполнения

**Проверка дублирования после Этапа 6:**
- [ ] Проверить, что все менеджеры имеют уникальную ответственность
- [ ] Убедиться, что нет дублирования функциональности между менеджерами
- [ ] Проверить, что интерфейсы менеджеров согласованы
- [ ] Запустить `fawltydeps` для проверки зависимостей

**Приоритет:** 🟢 MEDIUM

---

### ⏳ Этап 7: Создание Unit Tests
**Статус:** Ожидает выполнения

**Проверка дублирования после Этапа 7:**
- [ ] Проверить, что тесты покрывают все модули
- [ ] Убедиться, что нет дублирующихся тестов
- [ ] Проверить, что все TODO/FIXME в тестах закрыты
- [ ] Запустить `fawltydeps` для проверки зависимостей

**Цель:** Покрытие кода ~60-70%

**Приоритет:** 🟢 MEDIUM

---

### ⏳ Этап 8: Документация и CI/CD
**Статус:** Ожидает выполнения

**Проверка дублирования после Этапа 8:**
- [ ] Проверить, что документация не содержит дублирования
- [ ] Убедиться, что все TODO/FIXME закрыты
- [ ] Запустить `fawltydeps` для проверки зависимостей
- [ ] Проверить CI/CD конфигурацию на дублирование правил

**Приоритет:** 🟢 MEDIUM

---

## Этап 9: UI Quality (диагностика и исправление ошибок UI)

**Дата:** 2026-08-25
**Статус:** ✅ Завершён

### Инструмент аудита (регрессионная защита)
- Флаг `--audit-ui`: без рендера сверяет реестры меню/команд/хоткеев/окон;
  код выхода 2 при ошибках. `scripts/ci.sh` = сборка → ctest → аудит.
- `UiAuditor` + 8 юнит-тестов (`tests/core/test_ui_auditor.cpp`).
- Реализованы мёртвые API: `getAllMenuNames()`, `findMenuItem()`,
  `check_func` у пунктов меню.

### Персистентность
- `Settings::save_to_ini` была заглушкой с рождения (settings.ini всегда
  0 байт) — реализованы зеркальные save_* для ~230 полей.
- Раскладка окон: ini-blob ImGui и z-order внутри workspace JSON
  (`__last_session__`); SIGTERM/SIGINT завершают GUI с сохранением сессии.
- В облачном режиме реконнект к локальному серверу не выполняется.

### Меню и настройки
- 15 команд-заглушек подключены к реальной функциональности, 10 мёртвых
  пунктов удалено, 5 недостижимых stdout-команд убрано из CommandManager.
- Оживлена вкладка Security настроек (`render_security_settings` был
  мёртвым кодом); фикс ID-конфликта ImGui («Access Token» ×2).
- `verify_ssl` проведён во все curl-подключения (дефолт OFF — локальный
  сценарий), токен → `Authorization: Bearer` при старте/реконнекте.
- Починена система диалогов DialogManager (BeginPopupModal без OpenPopup —
  Info/Error/Warning никогда не показывались).
- Дедупликация пунктов плагинов в меню Agents; History Tab читает
  реальные данные bench_results/history.json.

### Метрики до → после
| Метрика | Было | Стало |
|---|---|---|
| Предупреждения аудита (заглушки/дубли) | 26 | 0 |
| Пунктов меню | 98 | 88 (мёртвые удалены) |
| Команд зарегистрировано | 117 | 102 |

### Остаток бэклога (не критично)
- Сирота-класс `PerformanceMonitor` (~500 строк): методы рендера никто
  не вызывает — включить в цикл или удалить.
- Настройка `show_performance_overlay` декоративна (не влияет на рендер).

---

## Этап 10: Система агентов (инструментов) для ядра

**Дата:** 2026-09-01
**Статус:** ✅ Завершён

### Суть
Вынос инструментов из плагина `wp_coder` в ядро приложения. Создана универсальная система из 12 агентов с единой архитектурой (IAgent), регистрацией через AgentRegistry и командами `/...` в чате.

### Что сделано
- 12 агентов: file, edit, glob, grep, code, terminal, rag, web_search, web_render, summarization, todowrite, question
- 12 команд чата: `/file`, `/edit`, `/glob`, `/grep`, `/code`, `/terminal`, `/rag`, `/search`, `/summarize`, `/todo`, `/question`, `/agents`
- Агенты компилируются статически (удалён PluginLoader)
- LLM-интеграция: `code_agent` и `summarization_agent` вызывают LLM через `context->llm_complete()`
- Безопасность: `project_root` restriction — файловые агенты блокируют доступ за пределы проекта
- Облачный режим: команды `/...` перехватываются даже при облачном провайдере

### Документация
Полный план: `docs/AGENT_TOOLS_PLAN.md`

---

## Процесс проверки после каждого этапа

### Шаг 1: Проверка TODO/FIXME
```bash
grep -rn "TODO\|FIXME\|XXX\|HACK" src/ include/
```
- Устранить все TODO/FIXME из кода
- Закрыть или удалить комментарии с FIXME

### Шаг 2: Проверка топ-10 крупнейших файлов
```bash
find src include -name "*.cpp" -o -name "*.h" | \
xargs wc -l | sort -rn | head -10
```
- Проверить, нет ли файлов > 700 строк
- Если есть - запланировать разбиение

### Шаг 3: Проверка дублирования в похожих файлах
```bash
# Сравнить похожие классы
diff -u src/core/rag_manager_deep_analysis.cpp src/core/rag_manager_hybrid.cpp

# Найти дублирующиеся паттерны
grep -r "void.*::" include/ | sort -u
```
- Сравнить похожие классы/функции
- Найти дублирующиеся паттерны кода

### Шаг 4: Генерация отчета
- Сохранить результаты в `docs/DUPLICATION_REPORT.md`
- Отметить файлы, требующие разбиения
- Предложить план действий
- Обновить чек-лист для следующего этапа

---

## Чек-лист проверки дублирования кода

### После каждого этапа:

#### 🔴 Этап 3: Рефакторинг MainWindow
- [ ] Проверить, что MainWindow разбит на менеджеры
- [ ] Проверить, что нет TODO/FIXME в MainWindow
- [ ] Проверить, что менеджеры не дублируют функциональность
- [ ] Запустить `fawltydeps` для проверки зависимостей

#### 🟡 Этап 4: LlamaBenchDialog
- [ ] Проверить, что каждая вкладка вынесена в отдельный класс
- [ ] Проверить, что нет TODO/FIXME в LlamaBenchDialog
- [ ] Проверить, что нет дублирования логики между вкладками

#### 🟡 Этап 5: Разбиение core классов
- [ ] Проверить `settings_ini.cpp` - разбить на парсеры
- [ ] Проверить `openrouter_client.cpp` - вынести HTTP-логику
- [ ] Проверить `rag_manager` - разделить по типам поиска
- [ ] Проверить, что нет TODO/FIXME в новых классах

#### 🟢 Этап 6: Заполнение managers
- [ ] Проверить, что все менеджеры имеют уникальную ответственность
- [ ] Проверить, что нет дублирования функциональности

#### 🟢 Этап 7: Unit Tests
- [ ] Проверить, что все модули покрыты тестами
- [ ] Проверить, что нет дублирующихся тестов

#### 🟢 Этап 8: Документация и CI/CD
- [ ] Проверить, что документация не содержит дублирования
- [ ] Проверить CI/CD конфигурацию

---

## При обнаружении дублирования

### Шаг 1: Определить причину
- [ ] Быстрый прототип (необходимо закончить)
- [ ] Недоделанный рефакторинг
- [ ] Несовершенная архитектура
- [ ] Временное решение

### Шаг 2: Создать план
- [ ] Определить, что нужно разбить/объединить
- [ ] Создать план разбиения (архитектура новых классов)
- [ ] Оценить время на рефакторинг
- [ ] Определить приоритеты

### Шаг 3: Выполнить рефакторинг
- [ ] Создать новые заголовочные файлы
- [ ] Создать новые файлы реализации
- [ ] Обновить CMakeLists.txt
- [ ] Выполнить рефакторинг поэтапно
- [ ] Проверить, что код работает

### Шаг 4: Проверить результат
- [ ] Проверить, что дублирование устранено
- [ ] Проверить, что нет TODO/FIXME в новом коде
- [ ] Запустить тесты
- [ ] Обновить документацию
- [ ] Добавить в чек-лист текущего этапа

---

**Важно:** Проверка дублирования должна выполняться **после каждого этапа**, а не по расписанию. Это гарантирует, что дублирование не накапливается со временем.

---

## Итоговая структура проекта (целевая)

```
llama-gui/
├── include/
│   ├── agents/       # Агенты (~7 заголовков)
│   ├── bench/        # Benchmarks (~5 заголовков)
│   ├── core/         # Ядро (~30 заголовков, после разбиения)
│   ├── ui/           # UI (~40 заголовков, после разбиения)
│   ├── managers/     # Менеджеры (~6 заголовков)
│   └── services/     # Сервисы (~5 заголовков)
├── src/
│   ├── agents/       # Агенты (~7 файлов)
│   ├── bench/        # Benchmarks (~5 файлов)
│   ├── core/         # Ядро (~30 файлов, после разбиения)
│   ├── ui/           # UI (~40 файлов, после разбиения)
│   ├── managers/     # Менеджеры (~6 файлов)
│   └── services/     # Сервисы (~5 файлов)
├── tests/            # Unit tests
└── CMakeLists.txt
```

---

## Сравнение до/после

| Метрика | До | После Этапа 2 | После Этапа 3-5 | Итог |
|---------|-----|---------------|-----------------|------|
| Файлов в src | ~200 | ~124 | ~90 | ~70 |
| Время полной сборки | ~5-10 мин | ~2-3 мин | ~1-2 мин | ~1 мин |
| Время сборки одного файла | ~30 сек | ~5 сек | ~3 сек | ~2 сек |
| Класс MainWindow | 2000+ строк | 905 строк | 300-400 строк | 300-400 строк |
| Покрытие тестами | ~10% | ~10% | ~30% | ~60-70% |
| Количество TODO/FIXME | ~100 | ~50 | ~20 | <10 |

---

## Приоритеты и сроки

### 🔴 CRITICAL (текущий этап)
1. **Этап 3: Рефакторинг MainWindow** (5-7 дней)
   - Разделить MainWindow на 4 менеджера
   - Приоритет: Высокий

### 🟡 HIGH (следующий этап)
2. **Этап 4: LlamaBenchDialog** (3-4 дня)
3. **Этап 5: Разбиение крупных классов core** (5-7 дней)

### 🟢 MEDIUM (после Этапа 5)
4. **Этап 6: Заполнение папки managers** (2-3 дня)
5. **Этап 7: Создание Unit Tests** (7-10 дней)
6. **Этап 8: Документация и CI/CD** (3-5 дней)

**Общее время:** 20-30 дней (3-4 недели)

---

## Риски и ограничения

### Риск 1: Слом работы при рефакторинге
**Минимизация:**
- Делать рефакторинг поэтапно с тестированием на каждом этапе
- Сохранять обратную совместимость где возможно
- Регулярная проверка дублирования кода

### Риск 2: Трудность в тестировании UI компонентов
**Минимизация:**
- Создать UI тесты с заглушками (mocks)
- Использовать интеграционные тесты для проверки UI

### Риск 3: Дублирование кода после рефакторинга
**Минимизация:**
- Регулярная проверка дублирования (каждые 2 недели)
- Использование инструментов для автоматического обнаружения
- Чек-лист проверки дублирования (см. выше)

---

## Приложение: Чек-лист проверки дублирования кода

### Еженедельная проверка
- [ ] Запустить `fawltydeps` для проверки неиспользуемых зависимостей
- [ ] Найти TODO/FIXME комментарии в коде
- [ ] Проверить топ-10 крупнейших файлов
- [ ] Сравнить похожие классы/функции

### Ежемесячная проверка
- [ ] Создать отчет о дублировании
- [ ] Предложить план действий для устранения дублирования
- [ ] Проверить git blame для крупных файлов
- [ ] Обновить документацию

### При обнаружении дублирования
1. Определить причину (быстрый прототип, недоделанный рефакторинг)
2. Создать план разбиения/объединения
3. Выполнить рефакторинг
4. Проверить, что дублирование устранено
5. Обновить документацию

---

## Продолжение работы: с чего начать

### Текущее состояние (обновлено 2026-09-01)

| Этап | Статус | Файлы |
|------|--------|-------|
| 1. Очистка мусора | ✅ Готово | - |
| 2. Модульные библиотеки | ✅ Готово | - |
| 3. MainWindow | ✅ Готово | `main_window.cpp` + 4 фрагмента |
| 4. LlamaBenchDialog | ✅ Готово | 4 класса вкладок |
| 5. Core классы | ✅ Готово | openrouter_client (230), rag_manager_deep_analysis (111) |
| 6. managers/ | ✅ Готово | 10 forwarding headers |
| 7. Unit Tests | ✅ Готово | 24 теста, 3 файла |
| 8. CI/CD | ✅ Готово | GitHub Actions + локальный CI |
| 9. UI Quality | ✅ Готово | Аудит UI, персистентность, меню |
| 10. Agent System | ✅ Готово | 12 агентов, 12 команд, безопасность |

### Все этапы завершены

| Приоритет | Описание |
|---|---|
| 🟢 MEDIUM | Тесты для новых агентов (edit, glob, grep, todowrite, question) |
| 🟢 MEDIUM | UI-виджет для todowrite/question (отдельная панель) |
| 🟢 LOW | Улучшение question_agent (блокирующий UI-диалог вместо echo) |

### Файловая структура MainWindow после Этапа 3

```
src/ui/main_window.cpp              (905 строк) — core lifecycle, init, InputHandler setup
src/ui/main_window_commands.cpp     (228 строк) — command registration
src/ui/main_window_core.cpp         (182 строк) — layout, status bar, fonts
src/ui/main_window_dialogs.cpp      (143 строк) — workspace, model dialogs
include/ui/window_coordinator.h     (37 строк)  — window rendering coordination
src/ui/window_coordinator.cpp       (22 строк)
include/ui/layout_controller.h      (214 строк) — layout management with callbacks
src/ui/layout_controller.cpp        (63 строк)  — layout implementation
include/ui/input_handler.h          (178 строк) — input handling with shortcuts
src/ui/input_handler.cpp            (153 строк) — input implementation
```

### Ключевые зависимости

- `WindowCoordinator` зависит от `WindowManager` (check visibility)
- `DialogManager` в `src/managers/` использует `include/ui/dialog_manager.h`
- `LayoutController` интегрирован — использует callback-based рендеринг для 3-панельного макета
- `InputHandler` интегрирован — регистрирует горячие клавиши и обрабатывает SDL2 события
- `dialog_manager_.render()` вызывается в `render_ui()` для рендеринга всех диалогов
- `render()` корректно делегирует в `render_ui()`

---

**Дата обновления:** 2026-07-18
**Следующая проверка:** 2026-07-31
