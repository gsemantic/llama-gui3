# Рефакторинг AI-кодера (wp_coder) — план работ

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` готово

Обновлять этот файл при каждом изменении статуса.

---

## Фаза A — стабильность и наблюдаемость (низкий риск, быстрые победы)

- [x] A1. Guard повторяющихся вызовов инструмента (одинаковый tool+args 3 раза — прерывание)
- [x] A2. Валидация инструмента перед запуском (неизвестный инструмент — короткий RESULT без раздувания контекста)
- [x] A3. Структурированные события шагов в чат (Tool/Assistant события форвардятся в chat_event)
- [x] A4. Вывод usage (prompt/completion токенов) в статистику шага

## Фаза B — качество решений

- [x] B1. Планирование: первый LLM-вызов — план действий перед исполнением (для сложных задач)
- [x] B2. Контекст-менеджер: кэш repo_map на задачу, резюме старых шагов вместо голой заглушки
- [x] B3. Структурированный протокол инструментов (JSON tool_calls) + обратная совместимость с wp_action
- [x] B4. Бюджет вывода на шаг (промежуточные ответы сжимаются до wp_action/JSON-блока)

## Фаза C — скорость и стоимость

- [x] C1. Стабильный префикс промпта для provider-side caching (инвариант + тест)
- [x] C2. Плавная сессия: резюмирование старых шагов вместо жёсткого порога 60 Кб (B2 — заглушки с именем инструмента и первой строкой)
- [x] C3. Кэш repo_map (не отправлять на каждом шаге, B2)

## Фаза D — рефакторинг кода

- [x] D1. Разбить run_task: Planner, AgentLoop, ToolRunner, PermissionGate, SessionStore (без goto)
  * Готово: SessionStore, PermissionGate, ToolRunner, Planner, AgentLoop реализованы в agent_components.{h,cpp};
    конструкторы inline в header; Engine::run_task делегирует Planner + AgentLoop.
    Сборка проходит, 48/48 тестов — ОК (2026-09-10).
  * Артефакт: libwp_coder.so (~670 Кб) собран и задеплоен.
- [x] D2. Вынести парсер протокола в tool_protocol.{h,cpp} с unit-тестами
- [ ] D3. Состояние агента как конечный автомат + observer-события; прерывание текущего HTTP-вызова
- [ ] D4. Тесты ReAct-цикла (fake llm_chat): шаги, бюджеты, повторы

## Фаза E — продукт/UX

- [ ] E1. Окно «Сессия»: список шагов (tool, args, статус, токены, $), отмена шага
- [ ] E2. Resume: сохранение сессии на диск и продолжение после прерывания/перезапуска
- [ ] E3. Отдельные настройки агента: лимит шагов, бюджет токенов, модель/провайдер
- [ ] E4. max_tokens для агента из настроек, а не жёсткие 4096

---

## Приоритет

A → B → C → D → E. A и B.1–B.2 дают основной эффект.